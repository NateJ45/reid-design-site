{
  "//": "Lighthouse CI runs against the built static output (dist/client) so dev-server quirks never affect scores. The url list is EXPLICIT on purpose: with staticDistDir alone, lhci auto-discovers pages but caps at 5, testing a near-random subset. Listing URLs covers every distinct template and skips non-page stubs. Accessibility is a HARD gate (must hold 100); performance / best-practices / SEO are warnings so normal CI variance doesn't block a PR. Not listed, on purpose: /studio, /preview/** and /api/draft-mode/* are SSR-only and never emitted into dist/client; the routes in tests/routes.ts hiddenRoutes are meta-refresh stubs (their section is switched off in Sanity) with nothing to audit; and /portfolio/[slug], /journal/[slug], /guides/[slug] and custom [slug] pages build zero paths on the token-less CI build. Add a representative URL per template as pages come online. Run locally: npm run build && npx lhci autorun",
  "ci": {
    "collect": {
      "staticDistDir": "./dist/client",
      "url": [
        "http://localhost/index.html",
        "http://localhost/about/index.html",
        "http://localhost/process/index.html",
        "http://localhost/services/index.html",
        "http://localhost/faq/index.html",
        "http://localhost/contact/index.html",
        "http://localhost/e-design/index.html",
        "http://localhost/privacy/index.html",
        "http://localhost/404.html"
      ],
      "numberOfRuns": 1,
      "settings": {
        "skipAudits": ["uses-http2", "canonical"]
      }
    },
    "assert": {
      "assertions": {
        "categories:accessibility": ["error", { "minScore": 1 }],
        "categories:best-practices": ["warn", { "minScore": 0.95 }],
        "categories:seo": ["warn", { "minScore": 0.95 }],
        "categories:performance": ["warn", { "minScore": 0.85 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 4500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
        "total-byte-weight": ["warn", { "maxNumericValue": 3000000 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
